export enum AUTH {
  BASE = "auth",
  LOGIN = AUTH.BASE + "/login",
  ME = AUTH.BASE + "/me",
  REGISTER = AUTH.BASE + "/register",
  MODIFYUSERDATA = AUTH.BASE + "/user",
  MODIFYPASSWORD = AUTH.BASE + "/password",
}
